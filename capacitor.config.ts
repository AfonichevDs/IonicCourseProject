import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.udemycoursdeproject',
  appName: 'ionic-course-project',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
